import { describe, it, expect } from "vitest";

describe("outbox strategy", () => {
  it("should mark processed_at after worker", () => {
    // stubbed test — real DB integration later
    expect(true).toBe(true);
  });
});
